<div class="tab-container">
  <button id="food-tab" data-clicked="n">Food</button>
  <button id="pets-tab" data-clicked="y">Pets</button>
  <button id="toys-tab" data-clicked="n">Toys</button>
</div>

<div data-state={{initial_state}} class="shop-window" id="shop-window">

  <h1 id="shop-title" class="center big-text">New Pets</h1>

  <div id="shop-pets" class="shop-items">
    {{#each shopPets}}
    <div class="emoji-container" data-price="{{price}}">
      <input type="image" data-price="{{price}}" id="{{_id}}" alt="{{emoji.name}}" src="{{emoji.img}}" />
      <p>{{price}} 💸</p>
    </div>
    {{/each}}
  </div>

  <div id="shop-food" class="shop-items">
    {{#each shopFood}}
    <div class="emoji-container" data-price="{{price}}">
      <input type="image" data-price="{{price}}" id="{{_id}}" alt="{{emoji.name}}" src="{{emoji.img}}" />
      <p>{{price}} 💸</p>
    </div>
    {{/each}}
  </div>

  <div id="shop-toys" class="shop-items">
    {{#each shopToys}}
    <div class="emoji-container" data-price="{{price}}">
      <input type="image" data-price="{{price}}" id="{{_id}}" alt="{{emoji.name}}" src="{{emoji.img}}" />
      <p>{{price}} 💸</p>
    </div>
    {{/each}}
  </div>

</div>

<div class="money">
  <p id="credits">{{money}} 💸</p>
</div>

<div class="center">
  <a href="/">Return to home page</a>
</div>