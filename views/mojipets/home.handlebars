<div class="left-sidebar">
  <div class="favorite-pets">
    <h2>Favorite Pets</h2>

    <div class="centered">
      <ul id="favorite-pets-ul">
        {{#each favorites}}
        <li>
          <input type="image" alt="{{emoji.name}}" src="{{emoji.img}}" />
        </li>
        {{/each}}
      </ul>
    </div>
    
    <div class="bottom">
      <a href="/pets" class="left">See all pets</a>
      <a href="/shop" class="right">Shop</a>
    </div>

  </div>

  <div class="inventory">
    <h2>Inventory</h2>

    <div class="centered">
      <ul id="inventory-ul">
        {{#each inventory}}
        <li>
          <input type="image" alt="{{emoji.name}}" src="{{emoji.img}}" />
        </li>
        {{/each}}
      </ul>
    </div>
    
    <div class="bottom">
      <a href="/inventory" class="left">See all items</a>
      <a href="/shop" class="right">Shop</a>
    </div>
    
  </div>
</div>

<div id="container"></div>

<div class="right-sidebar">
  <h2>Friends</h2>

  <ul id="friends-ul">
    {{#each friends}}
    <li>
      <div>
        <p>{{this.displayname}}</p>
        <form class="friend-form" method="GET" action="profile/{{this.username}}">
          <label>View Profile
            <input type="submit" value="view Profile" />
          </label>
        </form>
      </div>
    </li>
    {{/each}}
  </ul>

  <div class="centered bottom">
    <a href="/friends">See all</a>
  </div>
  
</div>

<div class="bottom-bar">
  <a id="logout" href="/logout">Log out</a>
</div>

<script defer="defer" src="/public/home.js"></script>